# Copy this file to backend/.env and fill in your values

# PostgreSQL connection string
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/flaskai

# For Docker Compose these are auto-set — only needed for local dev
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_DB=flaskai
